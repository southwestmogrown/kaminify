// Scraper utility — implemented in Issue #3
// Note: use `import * as cheerio from 'cheerio'` (not default import)
export async function scrapeSite(_url: string): Promise<never> {
  throw new Error('Not implemented')
}
